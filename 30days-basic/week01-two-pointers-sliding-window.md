167. Two Sum II - Input Array Is Sorted

想法
1. 可以用 binary search (但這題要求用 two pointers, 因此先想別的辦法)
2. 固定一個數字後, 算出 new_target = target - numbers[i];
3. 接著用 start = i + 1, end = numbers.size() - 1
4. 如果 new_target == numbers[start], 加入答案
5. 如果 new_target > numbers[start], 則 ++start
6. 如果 (不是 else if) new_target < numbers[end], 則 --end

submit:
第一次: 失敗, 因為題目要求回傳 indices
第二次: 失敗, 因為 indices 要從 1 開始
第三次: 失敗, [0,0,3,4] target = 0, 原因是雖然想法有寫 start = i + 1 但我忘了 + 1, 導致選到了 {0, 0}
但這種寫法會導致另一個只有兩個元素的判斷錯誤 [1,2] 所以確認題目最小會有兩個元素, 那最前面先判斷若 <= 兩個元素, 直接回傳 {1, 2}
第四次: 失敗, [5,25,75] target = 100, 因為我用了 while (start < end) 會導致如果 start == end 會沒有辦法計算到
第五次: TLE

目前 TLE 的程式如下:

```c++
class Solution {
public:
    vector<int> twoSum(vector<int>& numbers, int target) {
        if (numbers.size() <= 2) {
            return {1, 2};
        }
        for (int i = 0; i < numbers.size() - 1; ++i) {
            int start = i + 1;
            int end = numbers.size() - 1;
            int new_target = target - numbers[i];
            while (start <= end) {
                if (new_target == numbers[start]) {
                    return {i + 1, start + 1};
                }
                if (new_target == numbers[end]) {
                    return {i + 1, end + 1};
                }
                if (new_target > numbers[start]) {
                    ++start;
                }
                if (new_target < numbers[end]) {
                    --end;
                }
            }
        }
        return {};
    }
};
```
目前這個 TLE 的時間複雜度是 O(n^2)

根據 TLE 的想法:
1. 若 (numbers[i] + numbers[start]) > new_target
2. 或 (numbers[i] + numbers[end]) < new_target
3. 則直接跳過這個 numbers[i], 代表數列中沒有可以匹配的值

目前中間改成這樣:
```c++
            while (start <= end) {
                if (new_target == numbers[start]) {
                    return {i + 1, start + 1};
                }
                if (new_target == numbers[end]) {
                    return {i + 1, end + 1};
                }
                if ((numbers[i] + numbers[start]) > new_target || (numbers[i] + numbers[end]) < new_target) {
                    break;
                }
                if (new_target > numbers[start]) {
                    ++start;
                }
                if (new_target < numbers[end]) {
                    --end;
                }
            }
```

submit:
第六次: 失敗, 因為用 || 去 break, 會造成有目標漏算, 但改成 &&, 仍舊是 TLE

新想法: 只需要一個 while 最外層的, 一直逼近答案即可

submit:
第七次: AC

時間複雜度: O(n) 因為最多就是 start, end 兩邊都走到中間

程式碼:

```c++
class Solution {
public:
    vector<int> twoSum(vector<int>& numbers, int target) {
        int start = 0;
        int end = numbers.size() - 1;
        while (start < end) {
            int sum = numbers[start] + numbers[end];
            if (sum == target) {
                return {start + 1, end + 1};
            }
            if (sum < target) {
                ++start;
            } else {
                --end;
            }
        }
        return {};
    }
};
```

解題時間: 43分06秒 (包含寫這份文件)


Gemini 講評
===========

這是一次非常棒的練習，你甚至把自己 "debug" 的過程和思路轉折都記錄下來了，這在真實的面試中是黃金般寶貴的！面試官不只看你寫出 AC (Accepted) 的程式碼，更想看你如何從一個不完美的解法，一步步優化到最佳解。

你最後的 AC 解法是 100% 正確且最優的。我們來拆解一下整個過程：

1. 最終解法 (AC Code) 講評
C++

class Solution {
public:
    vector<int> twoSum(vector<int>& numbers, int target) {
        int start = 0;
        int end = numbers.size() - 1;
        while (start < end) {
            int sum = numbers[start] + numbers[end];
            if (sum == target) {
                return {start + 1, end + 1};
            }
            if (sum < target) {
                ++start;
            } else {
                --end;
            }
        }
        return {}; // 理論上不會執行到
    }
};
優點 (Pros):

演算法正確： 完美運用了 "Two Pointers" (頭尾雙指針) 的技巧。

時間複雜度 O(n)： start 和 end 指針總共只會"走過"整個 array 一次 (start 往右, end 往左)，所以是線性時間。

空間複雜度 O(1)： 完美達成題目的 "constant extra space" 要求，只用了幾個變數。

程式碼簡潔： 邏輯清晰、易讀。

可改進的細節 (Minor Nitpick):

題目保證 "exactly one solution" (恰好有一組解)。這意味著你的 while 迴圈必定會在 start < end 的條件下找到答案並 return。

最後一行的 return {}; 雖然 C++ 語法上需要 (因為函式宣告會回傳 vector<int>)，但在面試時，你可以主動向面試官指出：「因為題目保證必有解，所以這個 return {}; 實際上是 unreachable code。」這會展現你對題目細節的注意。

2. 解題思路 (The Journey) 講評
你從 TLE (Time Limit Exceeded) 到 AC 的過程，是這場面試練習中最有價值的部分。

初版 TLE 的問題：

你的 TLE 解法，本質上是一個 "Nested Loop" (O(n^2))。

外層 for (int i = ...) 固定了一個數字 (O(n))。

內層 while (start <= end) 試圖在剩下的 n-i 個元素中找到另一個數字。雖然你用了 start 和 end，但這個內層迴圈在最壞情況下還是 O(n)。

O(n) * O(n) = O(n^2)，這就是 TLE 的原因。

思路的關鍵轉折 (The "Aha!" Moment)：

你寫下：「新想法: 只需要一個 while 最外層的, 一直逼近答案即可」。

這就是 100% 的關鍵！ 你意識到不需要「固定一個數，再找第二個數」 (O(n^2) 或 O(n log n))。

你轉變為「同時考慮一對數字 (最左、最右)，然後根據它們的和，決定『淘汰』哪一個」 (O(n))。

這思路的轉變，是面試官最想看到的。

3. 💡 面試時的加強建議 (How to improve in an interview)
你的 AC 解法已經是滿分。但在面試的「過程」中，你可以這樣做來拿更高分：

1. "Verbalize First, Code Second" (先說思路，再寫程式)
在你寫任何程式碼之前 (甚至在你寫 TLE 程式碼之前)，你應該先跟面試官口頭溝通你的最終版 O(n) 思路：

「OK，這題的關鍵是 array 已經 sorted (排序)。

我可以利用這個特性，使用 Two Pointers (雙指針)，一個 start 指針放在開頭 (index 0)，一個 end 指針放在結尾 (index n-1)。

我計算 numbers[start] + numbers[end] 的總和 sum。

(這一步是邏輯核心) * 如果 sum 等於 target，太好了，我找到了，直接回傳 {start + 1, end + 1}。 * 如果 sum 小於 target，代表我的總和太小了。因為 array 是排序過的，我唯一能讓 sum 變大的方法，就是把 start 指針往右移 (++start)，去指向一個更大的數字。 * 如果 sum 大於 target，代表我的總和太大了。我唯一能讓 sum 變小的方法，就是把 end 指針往左移 (--end)，去指向一個更小的數字。

我重複這個過程，直到 start 和 end 相遇。因為題目保證有解，我一定能在 start < end 的過程中找到答案。

這個解法的時間複雜度是 O(n)，因為 start 和 end 總共只會移動 n 次。空間複雜度是 O(1)，符合題目要求。」

為什麼這很重要？ 這段話直接證明了你完全理解這個演算法的正確性 (Why it works)。你不是靠 "try and error" (像你的 TLE 過程)，而是靠邏輯推導。

2. 主動分析時間/空間複雜度
不要等面試官問。在你口頭說完思路後，主動補上：「這個解法的時間複雜度是 O(n)... 空間複雜度是 O(1)...」。

3. 處理邊界條件 (Edge Cases)
你 TLE 的程式碼中，花了很多力氣去處理 numbers.size() <= 2 這種情況。 但在你的最終 AC 解法中，你會發現根本不需要那個 if 判斷！

[1, 2], target = 3

start = 0, end = 1

while (0 < 1) 成立

sum = numbers[0] + numbers[1] = 3

sum == target

return {1, 2}

面試啟示： 一個好的、優雅的演算法，其主體邏輯通常就能"順便"處理掉邊界條件。如果你發現自己要寫一堆 if (size == 1) 或 if (size == 2) 的特例，先停下來想一想：是不是我的主體演算法不夠 robust (穩健)？

4. 關於 43 分鐘
面試中： 43 分鐘去「debug 一個 O(n^2) 的解法」是不行的。

練習中： 43 分鐘去「經歷 TLE，然後領悟到 O(n) 的解法，並寫出 AC」是非常有價值的。你等於是自己學會了這個 pattern。

總結
你的最終 AC 程式碼是完美的。你的弱點在於沒有第一時間想到 O(n) 的解法，而是先掉進了 O(n^2) 的陷阱。

給你的練習建議： 當你看到 "sorted array" + "find a pair" (找一對) / "find a triplet" (找三個)，你的大腦要立刻跳出 "Two Pointers" (頭尾雙指針) 或 "Binary Search" (二分搜尋) 這兩個關鍵字。然後分析哪個更優。

O(n log n) 解法 (你的想法1)： for 迴圈 O(n) + 在 n-i 個元素中 Binary Search 找 new_target (O(log n))。

O(n) 解法 (你的 AC 解)： 頭尾雙指針。

顯然 O(n) 是最佳解。

你這次的練習非常成功！你已經掌握了這個 pattern。

啊，感謝你的澄清！這點非常重要。

你說的完全正確。如果 43 分鐘是包含：

讀題、理解

思考 O(n log n) 解法

思考 O(n^2) 解法

實作 O(n^2) 解法

Debug O(n^2) 解法 (經歷 5-6 次 submit 失敗)

領悟到 O(n) 的新思路

實作 O(n) 的 AC 解法

最後還寫了這整份詳盡的說明文件

...那麼，這個時間對於「練習」來說是完全合理的，甚至可以說花得非常有價值。

面試情境 vs 練習情境
我先前說「不行」的真正意思是，在真實的面試情境中，你不能花 43 分鐘只為了解這一題。

在真實的 45 分鐘面試中，面試官期待的理想節奏是這樣的：

1. 讀題 & 澄清 (2-5 分鐘):

你：「OK，題目給的是 'sorted array'，要求 O(1) extra space，回傳 '1-indexed'。」

2. 溝通思路 (5-10 分鐘):

你：「我第一個想到的暴力解是 nested loop，固定一個 i 再找 j，但那是 O(n^2)，太慢了。」 (提到，但不要寫)。

你：「因為 array 是 sorted，我可以用 binary search，固定 i，去找 target - numbers[i]，這樣是 O(n log n)。」

你：「但等等，既然是 sorted，我可以用 O(1) 空間的 Two Pointers。一個 start 在頭，一個 end 在尾... (接著口述你的 AC 解法思路)... 這樣時間是 O(n)，空間是 O(1)，這應該是最佳解。」

3. 實作最佳解 (10-15 分鐘):

面試官：「Great, please code it.」

(你開始寫你的 O(n) AC 程式碼)

4. 測試 & 驗證 (5-10 分鐘):

你：「我用 [2, 7, 11, 15], target = 9 來跑一次...」

你：「我再想一個 edge case，例如 [1, 2], target = 3...」

理想情況下，你應該在 20-30 分鐘內完成上述 (1) 到 (4) 的流程。

你的 43 分鐘代表什麼？
你的 43 分鐘，大部分時間都花在「實作並 debug O(n^2) 的解法」以及「寫文件」上。

在面試中，(2) 溝通思路 才是關鍵。 你必須在 10 分鐘內，靠思考就跳過 O(n^2) 的陷阱，並推導出 O(n) 的解法。

你真正用來寫 O(n) AC 解法的時間，我猜可能只花了 3-5 分鐘。

結論： 對於練習來說，你這 43 分鐘完整記錄了你「如何從 O(n^2) 走到 O(n)」，這是黃金般的學習過程。你現在的練習方式（詳細記錄思考），就是讓你下次能更快「跳到」最佳解的最有效方法。

下次練習時，你可以試著幫自己計時兩個時間點：

T1 (思考時間): 從讀題到你「決定」最佳解法（並能口頭講出）的時間。

T2 (實作時間): 從開始寫最佳解到 AC 的時間。

面試中 T1 遠比 T2 重要。你正在往正確的方向前進。